# Handoff: сайт ПП ДЗВА (PPDZVA)

Снимок состояния на 2026-08-23 (обновлён после прохода по пункту 12 ТЗ — доступность), для продолжения работы в новой сессии/чате.

## Где что лежит

- Этот файл теперь лежит **внутри самого сайта** (`PPDZVA/handoff.md`) — свой `.git`, отдельный от внешнего репозитория `myProgect/`, в котором лежит эта папка.
- Текущая ветка: **`site-rebuild-vanilla`** — вариант дизайна "Инженерная консоль". Есть ещё две ветки-варианта (`design/swiss-minimalism`, `design/technical-blueprint`), решение, какую мержить/оставлять, **не принято**. Ветка **запушена на GitHub** (`origin/site-rebuild-vanilla`, репо `RidikylysClaude/PPDZVA`) — но там только `master`/`ppdzva`/`try_*`, PR в `master` **не создавать** без явного запроса (в ТЗ — мержить только после шагов 11–14).
- `../claude_development_t_z.md` (в корне `myProgect`, на уровень выше этой папки) — полное ТЗ на пересборку сайта. Читать перед любой крупной новой задачей по сайту.
- `../concurent_research.md` — бенчмаркинг конкурентов, исходный материал для ТЗ.
- Git-identity в этом репозитории настроена (`user.name=RidikylysClaude`, `user.email=pawn1738claude@gmail.com`) — коммитить можно без лишних вопросов.

## Стек и правила (кратко, подробно — в ../claude_development_t_z.md)

Vanilla JS/CSS, без сборщика, без jQuery, без Prepros. `window.DZVA` — общий namespace. Дизайн-токены в `css/tokens.css`. БЭМ. Header/footer/попап **дублируются в каждом HTML-файле** — при правке контактов/бренда синхронизировать во всех файлах.

**Шаблон карточки товара** — общий паттерн, уже отработан на 4 товарах (buvv/bpvv/rs80/digmat): `product-hero` (галерея + описание + аналог-бейдж + CTA) → `advantages` (6 карточек с SVG-иконками) → `product-specs` (таблица характеристик, 6–8 строк видно сразу + остальное за кнопкой `data-spec-toggle`, счётчик в кнопке **обязан** совпадать с реальным числом `<tr>`). Единицы измерения — в столбце «Значение», не в «Параметр». Для новых страниц копировать `products/buvv.html` как основу.

## Технические характеристики — как делать правильно

**Не копипастить текст таблиц из PDF напрямую** — PDF ломает порядок ячеек (значения "уезжают" от своих строк), доказано минимум дважды. Правильный процесс:
1. Спросить у пользователя номер страницы(-иц) с таблицей характеристик в РЭ.
2. Прочитать PDF напрямую (`pymupdf`/`fitz`, см. ниже — не поставлен по умолчанию) — либо `get_text()` для многостраничного текста (сохранять в файл через `python3 ... > scratch.txt`, иначе кириллица падает на `cp1252` в Windows-консоли), либо `page.get_pixmap(dpi=200-220).save(...)` и прочитать как картинку через `Read` — это надёжнее для самих таблиц (текст всё равно может "плыть").
3. Свериться визуально с картинкой перед тем, как вставлять цифры в HTML — визуальная проверка уже один раз поймала ошибку (в БУВВ изначально была вставлена таблица характеристик БПВВ по ошибке).
4. Считать реальное количество `<tr>` в итоговой таблице и вписать это число в `data-spec-toggle-label` — проверять через `grep -c "spec-table__row"` перед коммитом, а не полагаться на ручной подсчёт (уже расходилось на 1 в ДИГМАТ).

## Что сделано в последних сессиях

### Инфраструктура
- **python3 shim**: на этой машине `python3` отсутствовал в PATH (только `python`/`py`), из-за чего падали все хуки плагина `hookify` (Stop/PreToolUse/PostToolUse/UserPromptSubmit — жёстко зовут `python3`). Создан shim `C:\Users\Administrator\bin\python3` → `exec python "$@"` (эта директория уже первая в PATH). Чинит любые хуки/скрипты, зовущие `python3`, не только hookify.
- Для чтения PDF по страницам (`Read` с `pages=`) на этой машине нет `poppler`/`pdftoppm` — вместо этого ставится `pymupdf` (`python3 -m pip install pymupdf`) и рендерятся страницы вручную (см. процесс характеристик выше).

### Страница БУВВ-1 (`products/buvv.html`)
- Лайтбокс фото товара и лайтбокс скриншотов веб-интерфейса (`js/components/lightbox.js`, `webui-gallery.js`) — увеличены до ~700px высотой, закрытие теперь по клику в любом месте вне картинки/стрелок/миниатюр (раньше работал только крестик — `.lightbox` перекрывал `.overlay` по z-index, клик по "фону" на самом деле попадал в `.lightbox`, у которого не было обработчика).
- 6-я карточка веб-интерфейса «Настройки защиты» (скриншот панели токовой защиты).
- **Таблица характеристик исправлена** — раньше туда была по ошибке вставлена таблица БПВВ-1 (см. ниже, откуда взялась путаница). Актуальная таблица сверена с `content/RE_BUVV.pdf` (стр. 4–5), 24 строки.
- Адаптив герой-блока (фото+описание): брейкпоинт одноколоночного режима поднят с 1200px до **900px** (двухколоночная раскладка держится дольше), заголовок переносится только после «БУВВ-1» (убран `&nbsp;`), текст описания выключен по ширине (`text-align: justify`), отступ фото↔текст уменьшен (`--space-8` → `--space-6`), рамка вокруг фото увеличена (`--space-4` → `--space-5`, чтобы стрелки не заезжали на фото), кнопки CTA: на 901–1200px — столбик на всю ширину, на ≤900px — `flex:1 1 0` (растягиваются поровну, при нехватке места каждая уходит на свою строку).

### Страница БПВВ-1 (`products/bpvv.html`) — пункт 6 ТЗ
- Собрана по шаблону БУВВ. **Одна галерея** (5 фото, EXIF-повёрнуты из `img/BPVV_photo/*.JPG`) — на старой `BPVV.html` было два одинаковых карусели подряд, это баг, не переносили.
- 6 карточек преимуществ с реальными SVG-иконками (не `HarryPotter.svg`).
- Таблица характеристик (25 строк) сверена с `content/RE_BPVV.pdf` — это та самая таблица, что раньше по ошибке попала на страницу БУВВ.
- Карточка на главной стала кликабельной ссылкой; в тексте БУВВ упоминание «БПВВ-1» теперь ссылка (класс `.inline-link`, стиль в `components.css`).

### Страница РС-80/Д (`products/rs80.html`)
- 5 реальных фото (EXIF-повёрнуты из `img/RS_photo/*.JPG`, выбраны ракурсы с платой/клеммниками — у этого товара сложная схема подключения, фото несут техническую информацию).
- Таблица характеристик (27 строк) — реле сложнее по структуре, чем БУВВ/БПВВ (диапазоны уставок вместо плоских параметров, много ГОСТ-пунктов), сверена с `content/RE_RS-80D.pdf`.
- Преимущества делают акцент на: (1) прямой аналог **РС80М2М**, оборудование снято с производства; (2) статическое реле без доп. источника питания; (3) **функция дешунтирования — опция**, заказывается отдельно с ней или без (это не программный тумблер, а разное исполнение клеммника — см. п. 5.3.1/5.3.2 РЭ).

### Страница ЭИС-ЛН-220-К(Б) (`products/eis-ln-220-kb.html`)
- Собрана по шаблону ДИГМАТ (единственное фото, галерея на 1 слайд). Своя разработка, не аналог — на карточке и на странице badge `badge--original` "Собственная разработка" (компонент уже был в CSS, просто не использовался).
- **Фото нет** — вместо реального фото используется `img/zaglushka.jpg` (шуточная заглушка "фото скоро будет"), пересжата в `img/products/eis-ln-220-kb/eis1.webp` (~120KB). При появлении фото — заменить эту картинку и, при необходимости, добавить ещё `data-gallery-thumb` кнопки.
- Акцент в преимуществах — по прямому запросу пользователя: это ретрофит/апгрейд светового табло ТСБ (лампы накаливания под него больше не выпускают), устройство разрешает ставить светодиоды, но электрически имитирует переходной процесс лампы накаливания (бросок тока до 110 мА, плавно спадающий за ~1 сек до 2-5 мА через клемму XS1), чтобы существующие защиты на объектах (импульсные реле РТД, контроль исправности цепей) не видели подмену и продолжали работать без переделки схем. Второй режим — клемма XS2, обычный светодиодный индикатор без имитации.
- Таблица характеристик — 14 строк, сверена визуально с `content/RE_EIS.pdf` (стр. 4, единственная таблица в РЭ — сам документ короткий, 7 страниц). 8 видно сразу + 6 за `data-spec-toggle`.
- Карточка на главной (`index.html`) переведена из `product-card--pending` (div) в кликабельный `<a>`, тем же паттерном, что у digmat/rs80/buvv/bpvv. Счётчики в `products__finder-stats` ("6 готовых / 3 в разработке") **не менялись** — это счётчик карточек по секциям (в "Готовой продукции" всё ещё 6 карточек: 5 ссылок + КТРМ pending), а не факт готовности.
- Закоммичено и запушено (`96cc23e`).
- Визуально проверено в браузере (puppeteer): галерея с одним фото, раскрытие таблицы характеристик, клик по карточке на главной — всё работает.
- **Найдены и исправлены два бага после первого коммита** (см. `be19c36`, `56dfa71`):
  1. На широких экранах (>1200px) `.product-hero__inner`/`.product-hero__info` имеют фиксированную высоту (650px/600px) под текст в две колонки рядом с фото — у ЭИС текст был длиннее, чем у других товаров, и переполнял блок, наезжая на "Преимущества". Фикс: короткий абзац оставлен в двухколоночном блоке, детали (клеммы XS1/XS2) вынесены в новый параграф на всю ширину контейнера **под** блоком фото+текст (`.product-hero__desc--more`, max-width 80ch) — там фиксированной высоты нет, в обычном потоке документа. **Важно на будущее**: если у следующего товара длинное описание, либо сокращать текст в двухколоночном блоке (бюджет ~600px минус высота heading/replaces-box/actions), либо сразу использовать этот же паттерн выноса продолжения текста.
  2. Общий баг компонента галереи (не только ЭИС, был и на уже запущенном ДИГМАТ): на мобильном (`≤900px`) `.product-viewer__thumb` получает `aspect-ratio: 3/4`, рассчитанный на несколько миниатюр в ряд (`flex:1` делит между ними ширину). При **одном** фото в галерее (товар без доп. съёмки) `flex:1` растягивает единственную миниатюру на всю ширину полосы, и тот же aspect-ratio даёт огромную высоту — по сути дубль главного фото. Исправлено через `.product-viewer__thumb:only-child` — только для одиночной миниатюры возвращена невысокая полоса как на десктопе; многофотные галереи (rs80/buvv/bpvv) не затронуты, проверено визуально.
  3. Заголовок h1 «ЭИС-ЛН-220-К(Б)» (длиннее названий других товаров) переносился на 2 строки в двух диапазонах ширины: 901–~1065px (узкая двухколоночная зона) и ≤~400px (мобильный, где `--fs-h1` уже упирается в нижнюю границу clamp). Добавлен модификатор `.product-hero__title--long` (только на этой странице — у остальных названия короче, трогать не нужно), размеры подобраны эмпирически через бинарный поиск в браузере (puppeteer): 1.9rem для 901–1200px, 1.6rem для ≤410px. **На будущее**: если у следующего товара (КТРМ и т.д.) длинное название — сначала проверить перенос на этих же двух диапазонах ширины (обычно на границах 900↔901 и 400↔410px), при необходимости завести свой `--long`-модификатор с новыми подобранными значениями (не переиспользовать готовые 1.9rem/1.6rem вслепую — они подобраны под конкретную длину строки «ЭИС-ЛН-220-К(Б)»).

### Страница ДИГМАТ (`products/digmat.html`)
- **Фото пока нет** — пользователь попросил зарезервировать место под будущую галерею, а сейчас использовать только `digmat-card.png` (пересжат в `img/products/digmat/digmat1.webp`, ~100KB вместо 6MB, альфа-канал сохранён). Разметка галереи собрана по тому же паттерну (data-gallery-thumb), для добавления новых фото в будущем достаточно добавить кнопки — JS/CSS менять не нужно.
- **Найден и исправлен реальный баг общего компонента**: при количестве фото <2 `gallery.js`/`lightbox.js` проставляют атрибут `hidden` на стрелки/миниатюры, но `display: flex` в CSS (`.product-viewer__nav`, `.lightbox__nav`, `.lightbox__thumbs`) перебивал нативный `[hidden]{display:none}` — раньше не проявлялось, т.к. у всех прежних товаров было ≥2 фото. Добавлены явные `[hidden]{display:none}` правила в `components.css`; `lightbox.js` доработан — теперь тоже прячет свои стрелки/миниатюры при одном фото (раньше это делал только `gallery.js` для стрелок в рамке).
- Таблица характеристик (29 строк) сверена с `content/RE_DIGMAT.pdf` — реле цифровое, программируемое, для цепей и переменного (по трансформаторам тока), и постоянного тока (по шунту), 3 канала связи (RS-485/Modbus, Ethernet, CAN).
- Преимущества: аналог **DIGmat S100**, работа AC+DC, цифровая настройка уставок, 3 канала связи, гальваническая развязка входов (2 кВ), IP65 лицевой панели.
- Карточка на главной стала кликабельной. Закоммичено (`eb0545f`).

### Страница `analogs.html`
- Сводная таблица аналогов (пункт 9 ТЗ, раздел 3а) — 5 строк: BU/TEL-220-05A→БУВВ-1, BP/TEL-220-02A→БПВВ-1, РС80М2М→РС-80/Д, DIGmat S100→ДИГМАТ, ABB REF615→REF615 (badge `badge--dev`, продукта ещё нет). ЭИС и КТРМ **не включены** — это собственные разработки без прямого зарубежного аналога, не подходят под паттерн «искали X → у нас есть Y»; про них отдельная сноска `.analogs-note` со ссылкой на `index.html#products`.
- Клиентский поиск/фильтр (`js/components/analogs-table.js`) — тот же паттерн, что `product-search.js`: фильтрует `<tr>` по `textContent`, при 0 совпадений скрывает таблицу и показывает пустое состояние с CTA «Оставить заявку» (по совету ui-ux-pro-max: не оставлять тупик без подсказки).
- Новый статус-бейдж `.badge--dev` (нейтральный серый, components.css) — для «в разработке», отдельно от зелёного `badge--analog`/амбер `badge--original`, специально без цвета "готовности", чтобы не читался как доступный товар. У него добавлен `white-space: nowrap` (в отличие от общего `.badge`, который намеренно допускает перенос ради `.badge__code` на карточках товара) — иначе «В разработке» ломалось на 2 строки в узкой колонке «Статус».
- **Рефактор перед версткой:** `.breadcrumbs*` и `.inline-link` были в `product.css` (только для `products/*.html`), но нужны и на `analogs.html` — перенесены в `components.css` как общие компоненты. Визуально ничего не изменилось (components.css уже подключался раньше product.css везде), проверено скриншотом на БУВВ после переноса — регрессий нет.
- **Таблица на узких экранах (≤760px)** — НЕ полный постолбцовый стек (как `.spec-table`), а сетка: «Искали»/«У нас есть» бок о бок в две колонки сверху, «Категория» и «Статус» каждая на всю ширину строки под ними (`grid-template-areas`, привязка колонок через `td[data-label="..."]` — не зависит от DOM-порядка). Весь текст внутри ячеек центрирован (`text-align:center` на `.analogs-table__row td`). Это по прямому запросу пользователя после ревью на 713px — полный стек был слишком высоким и не использовал горизонтальное место.
- **Диаграмма pin-to-pin в интро-блоке** (по запросу пользователя, страница выглядела пусто без картинки) — inline SVG (`viewBox 0 0 360 200`), двухколоночная раскладка (текст+поиск слева, диаграмма справа, схлопывается на ≤900px, там же `.analogs-diagram`/`.analogs-intro__visual-labels` ограничены `max-width:500px` — на схлопнутой раскладке диаграмма чисто иллюстративная, без интерактива, во всю ширину колонки смотрелась слишком крупной). Два контура корпусов **намеренно разного размера/пропорций** — совпадает только раскладка клемм снизу (4 одинаковых по интервалу клеммы у обоих). **Важно:** пользователь явно поправил — НЕ утверждать одинаковые посадочные размеры (они отличаются), только совпадение разводки клемм. Подпись под диаграммой: «Идентичная разводка клемм — интеграция без изменения проектной документации и повторных согласований» — без слова «посадочные».
  - Подписи «Снято с производства» / «ПП ДЗВА» под диаграммой — сетка `grid-template-columns` с пропорциями, зеркалящими сегменты viewBox диаграммы (сейчас `90fr 150fr 100fr 20px`), а не единая центрированная строка — иначе подписи читаются слитной фразой и/или уезжают от своего блока на широких экранах. Правый крайний отступ — фиксированные `20px` (по прямому запросу, не проценты); симметричный левый отступ добавлял ненужный сдвиг у «Снято с производства» и был убран.
  - Формат диаграммы выбирался через `AskUserQuestion` (варианты: SVG-диаграмма / реальные фото / без картинки / сгенерированное фото) — выбрана SVG-диаграмма; консультировался с ui-ux-pro-max по паттернам таблиц/поиска до этого.
- Визуально проверено в браузере (puppeteer) на всех правках: десктоп (1400/1920px), ~900px, 713px, 390px, поиск (фильтрация и пустое состояние), клик CTA в пустом состоянии открывает попап заявки — регрессий не найдено.
- Все правки закоммичены и запушены: базовая страница (`6861ff8`), диаграмма (`9d8b3ff`, `159c42a`, `d351bb9`, `f0e9b77`, `622fae6`, `a6632dd`), бейдж `badge--dev` nowrap (`b0a5e1b`), 2-колоночная таблица + центрирование (`145f29a`, `064d716`).

### Пункт 11 ТЗ — адаптивная вёрстка (пройдено на всех 7 существующих страницах)
- Проверены `index.html`, `analogs.html`, `products/{buvv,bpvv,rs80,digmat,eis-ln-220-kb}.html` — mobile (320–410px), tablet (560–900px), desktop (901–1920px), с точечной проверкой реальных CSS-брейкпоинтов страницы (не случайных ширин) через `grep @media` по подключённым файлам. КТРМ пока не существует (см. «Не начато»), поэтому не проверялся.
- Метод: puppeteer, `document.documentElement.scrollWidth` vs `window.innerWidth` на каждой ширине (ловит горизонтальный overflow по всему документу, а не только по видимой области экрана) + точечные визуальные скриншоты по секциям (герой, галерея/лайтбокс, таблица характеристик со счётчиком, попап заявки, бургер-меню, футер).
- **Найдено и исправлено:**
  1. `index.html:114` — строка доверия под hero содержала опечатку «С 2024 года» вместо «С 2014 года» (расходилось с фактом в блоке «О компании», строка 256, где верно «С 2014 года»). Единственное вхождение на сайте, исправлено.
  2. Общий баг вёрстки: h1/h2/h3/h4 не переносили длинные слова (`overflow-wrap: normal` по умолчанию) — на `analogs.html` при 320px слово «оборудования» в заголовке H1 вылезало за пределы экрана (`scrollWidth` 331 vs `innerWidth` 320, обнаружено через `element.scrollWidth`/`clientWidth`, а не через `getBoundingClientRect`, — последний не ловит overflow текста внутри элемента, если сам блок не растянут). Исправлено добавлением `overflow-wrap: break-word;` в общее правило `h1,h2,h3,h4` в `css/base.css` — затрагивает весь сайт, регрессий на других страницах/брейкпоинтах не найдено (перепроверено на 320px на всех 7 страницах).
  - **Важно на будущее:** при локальной проверке через `python -m http.server` браузер puppeteer агрessивно кэширует CSS между навигациями на localhost (обычный `location.reload()`/query-param на URL страницы не помогает, т.к. URL самого `.css`-файла не меняется). Если правишь CSS и хочешь тут же перепроверить в том же puppeteer-сеансе — либо перезапускай сервер/браузер, либо подменяй `<link href>` на новый URL с `?v=timestamp` через `puppeteer_evaluate` (пример есть в истории сессии).
- **Не найдено регрессий** (не потребовало правок): двухколоночная раскладка hero карточек товара на брейкпоинте 901–1200px (включая длинный заголовок ЭИС на 901/1065/1200px — уже пофикшено в прошлой сессии, всё ещё работает), переключение галерея↔лайтбокс на мобильном, счётчики `data-spec-toggle-label` совпадают с реальным числом строк на всех 5 товарах (24/25/27/29/14), сетка карточек `analogs.html` (card-режим ≤760px ↔ таблица ≥761px), диаграмма pin-to-pin (одна/две колонки на 900/901px), пустое состояние поиска на `analogs.html` и поиска по продукции на `index.html`, бургер-меню и попап заявки идентичны на всех проверенных страницах.
- **Не проверялось в рамках этого прохода** (вне периметра пункта 11, оставлено на пункты 12–14): доступность (контраст, aria, keyboard-nav — только `aria-expanded` на spec-table спорадически замечен рабочим, отдельного аудита не было), Lighthouse/производительность, кроссбраузерность (тестировалось только в headless Chrome через puppeteer).

### Пункт 12 ТЗ — доступность (пройдено на всех 7 существующих страницах)
- **Общий focus-trap (новый `js/components/focus-trap.js`)** — подключён на всех 7 страницах перед `popup.js`. Даёт `window.DZVA.trapTabKey(dialog)` (Tab зациклен внутри диалога, пока открыт) и `window.DZVA.focusFirst(dialog)` (фокус на первый фокусируемый элемент диалога через `requestAnimationFrame` — без rAF `element.focus()` иногда молча не срабатывает сразу после `classList.add('active')`, пока браузер не пересчитал стили; поймано и подтверждено через puppeteer). Переиспользован в трёх диалогах — `popup.js`, `lightbox.js`, `webui-gallery.js`: при открытии фокус уходит в диалог и запирается там, при закрытии — возвращается на элемент, с которого диалог открыли (`lastFocused = document.activeElement` в момент открытия).
- **Реальный баг: `.product-viewer__stage` (рамка фото с фото+стрелками) была недоступна с клавиатуры** — просто `<div>` с обработчиком только на `click`, лайтбокс через Tab+Enter открыть было нельзя вообще. Исправлено: `role="button" tabindex="0" aria-label="Открыть увеличенное изображение"` на самом `.product-viewer__stage` (5 товарных страниц), плюс `keydown`-обработчик Enter/Space в `lightbox.js` — с проверкой `event.target !== stage`, иначе Enter на вложенных кнопках "Пред./След." тоже всплывал бы и лишний раз открывал лайтбокс.
- **Skip-link** — `<a href="#main" class="skip-link">Перейти к основному содержимому</a>` первым элементом в `<body>` на всех 7 страницах, `id="main" tabindex="-1"` на `<main>` (tabindex="-1" — чтобы фрагмент-переход реально передавал фокус, а не просто скроллил). Стили в `base.css` (`.skip-link`, `.skip-link:focus-visible`) — обычная невидимая-до-фокуса ссылка. Раньше клавиатурный пользователь был обязан таб-нуться через весь header/nav на каждой странице (WCAG 2.4.1).
- **Убран `outline: none`, ломавший видимый фокус** на `.products__finder-input` (`home.css`) и `.analogs-search__input` (`analogs.css`) — оба правила глушили общий `:focus-visible { outline: 2px solid ... }` из `base.css`, оставляя только едва заметную смену цвета рамки. `outline: none` убран, `border-color` при фокусе оставлен как есть (дополнение, не замена).
- **`aria-live="polite"` регионы для живого поиска** — `[data-search-status]` на `index.html` (`product-search.js`) и `[data-analogs-status]` на `analogs.html` (`analogs-table.js`), визуально скрыты (`.visually-hidden`), озвучивают "Найдено позиций: N" / "Найдено строк: N" / "Ничего не найдено" при фильтрации (WCAG 4.1.3 — раньше результат фильтра был виден только зрячим).
- **Esc закрывает мобильное меню** и возвращает фокус на кнопку-бургер (`burger-menu.js`) — для параллели с попапом/лайтбоксом, раньше Esc не работал на бургер-меню вообще.
- Всё проверено в puppeteer: фокус-трап (Tab/Shift+Tab зациклены), возврат фокуса при закрытии попапа/лайтбокса на реальный `puppeteer_click` (не JS `.click()` — синтетический клик через JS в headless не всегда даёт реальный фокус, поймано и учтено при тестировании), клавиатурное открытие лайтбокса через Enter на `.product-viewer__stage`, живые регионы поиска, отсутствие `outline: none`.
- **Не проверялось** (не входит в п.12, оставлено на п.13): Lighthouse-аудит, цветовой контраст проверен точечно вручную по токенам (`--color-text-muted`, `badge--*`, `btn--gold` — все проходят AA), полного автоматизированного аудита (axe/Lighthouse a11y) не было.

### Навигация меню (все страницы)
- `js/components/nav-active.js` — scroll-spy без rAF (в headless-браузере rAF иногда не срабатывает).
- `js/main.js` — `runInit(name)`, безопасно дёргает `window.DZVA.initXxx()` только если функция существует.

### ⚠️ Изображения и PDF не в git
`.gitignore` этого репозитория игнорирует **весь `img/`** и **все `*.pdf`** — все фото (БУВВ/БПВВ/РС-80/ДИГМАТ) и все РЭ в `content/*.pdf` лежат только на диске, в git не закоммичены. При переносе на другую машину/клонировании репо эти файлы потеряются. Решение осознанное ("чтобы Клод не читал бинарный код"), пересматривать не обязательно, но помнить при клонировании на новую машину.

## Git-статус на момент записи

- Закоммичено и запушено: каркас (`9bd438b`), доводка БУВВ + БПВВ (`48b2040`), удаление переименованного вручную файла (`7999a29`), РС-80/Д (`9474ea8`), ДИГМАТ + фикс `[hidden]` (`eb0545f`), ЭИС-ЛН-220-К(Б) (`96cc23e`), фиксы ЭИС (`be19c36`, `56dfa71`, `bb9c143`), `analogs.html` + рефактор breadcrumbs/inline-link (`6861ff8`), диаграмма и её доводка (`9d8b3ff`…`a6632dd`, см. секцию `analogs.html` выше), `badge--dev` nowrap (`b0a5e1b`), 2-колоночная таблица на узких экранах + центрирование (`145f29a`, `064d716`), пункт 11 ТЗ — фикс опечатки года в trust-strip и `overflow-wrap` для заголовков (`88341c9`).
- Рабочее дерево чистое, ветка синхронизирована с `origin/site-rebuild-vanilla` (последний коммит на момент записи — см. `git log -1`).
- **Этот файл (`handoff.md`) перенесён из внешнего `myProgect/` внутрь этого репозитория** — раньше жил на уровень выше и не коммитился вместе с сайтом (внешний репозиторий `myProgect/` вообще без единого коммита). Теперь трекается и пушится вместе с остальными изменениями сайта.

## Не начато (из ТЗ, ../claude_development_t_z.md)

- Страница-заготовка: КТРМ (РС-80, ДИГМАТ, ЭИС, `analogs.html` уже сделаны). Когда появится — пункт 11 (адаптив) нужно будет пройти и для неё отдельно, остальные 7 страниц уже проверены.
- 3D-модуль (`<model-viewer>`, glTF/GLB) — сейчас fallback-галерея фото используется постоянно, не временно.
- SEO-проход: `sitemap.xml`, `robots.txt` (title/description/OG уже есть на каждой товарной странице по мере создания).
- **Пункт 13 ТЗ — производительность**: WebP/lazy частично уже используется по ходу разработки страниц, но отдельного Lighthouse-прохода не было.
- **Пункт 14 ТЗ — SEO + кроссбраузерность + уборка**: meta-теги на страницах уже проставлены по шаблону при создании, но `sitemap.xml`/`robots.txt` не созданы; кроссбраузерность за пределами headless Chrome не проверялась; уборка дублей изображений/orphaned файлов не делалась.
- Решение, какую из трёх дизайн-веток мержить как основную.

## Как продолжать

1. Прочитать этот файл + `../claude_development_t_z.md`.
2. `git status` в этой папке — на момент записи рабочее дерево чистое, всё запушено.
3. Дальше — по приоритету пользователя: страница-заготовка КТРМ по шаблону (см. процесс работы с PDF и паттерн карточки товара выше), либо пункты 13–14 ТЗ (производительность/SEO-проход, см. выше). Пункт 12 (доступность) пройден.
